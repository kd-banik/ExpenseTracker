// const express = require("express"),
// app = express(),
// md5 = require("md5"),
// mysql = require("mysql"),
// cookie_parser = require("cookie-parser");

// port = process.env.PORT || 3000;    //to be altered

let connectionObject = {
    host : "",
    user : "",
    password: "",
    database: "",
    port:3306
}
module.exports = connectionObject;